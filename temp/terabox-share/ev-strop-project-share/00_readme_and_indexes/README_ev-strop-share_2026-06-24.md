# EV Strop Share Package

Package root for shared project materials intended for `TeraBox`.

Public share link:

- `https://1024terabox.com/s/1KCpFJslTQu2x32TrnptnRA`

Current priority package:

- `07_compatibility_check/2026-06-24_module-01_sync/`

Purpose:

- share project documentation with colleagues;
- provide editable `PowerPoint` sources;
- provide clickable `PPSX` review builds;
- provide required media for compatibility checks;
- prepare the same structure for future `iSpring` build handoff.

Rules:

- keep shared package names in ASCII only;
- do not rename `working` and `review` files manually;
- treat `powerpoint-working` as editable source;
- treat `review-builds` as view-only files;
- update manifest before publishing a new package version.
