# Module 01 Compatibility Package

Package ID:

- `2026-06-24_module-01_sync`

Share link:

- `https://1024terabox.com/s/1KCpFJslTQu2x32TrnptnRA`

Goal:

- verify compatibility between our `PowerPoint` work and colleague workflows;
- confirm that required files for module 01 are available for future `iSpring` assembly.

What to review:

- `docs/` for structure, naming rules, and handoff logic
- `powerpoint-working/` for editable source files
- `review-builds/` for clickable review files
- `media-required/` for all files needed to reproduce and assemble current module-01 work

Important:

- edit only files in `powerpoint-working/`
- use `review-builds/` only for checking clicks and viewing
- do not rename package files after upload
- if a file is replaced, bump its version and update manifest
