# Готовое наполнение слайдов: Тема 1 `Общая часть`

Дата фиксации: `2026-06-18`

## Назначение

Это готовый рабочий пакет наполнения для `Темы 1. Общая часть`.

В документе собраны:

- готовый текст для экрана;
- готовая логика озвучки;
- подсказка по визуалу;
- переходы и интерактив.

Документ подготовлен уже не на уровне тезисов, а на уровне содержимого, которое можно переносить в `PowerPoint` и `iSpring`.

## Состав темы

В пакет входят:

- `S001-S006` — основные линейные слайды;
- `S003-P01...P04` — справочные выносы по документам и правилам;
- `S004-P01` — вынос по этапам работы стропальщика;
- `S005-P01...P03` — выносы по инструктажу, проверке знаний и СИЗ;
- `S006-P01` — пояснение по видам грузов;
- `S006-P56-01` и `S006-P56-01-PP01` — вводный контур `5-6 разряд`.

## `S001` Титульный

Экран:

- ЭВ Стропальщик
- Стропальщик `2-4 разряда`
- Стропальщик `5-6 разряда`
- Практический курс по безопасной работе с грузами
- Базовая часть курса - для `2-4` разряда. Для `5-6` разряда открывайте дополнительные блоки.

Озвучка:

- Этот курс предназначен для подготовки и повторения основных правил работы стропальщика.
- Базовая часть курса рассчитана на `2-4 разряд`.
- Для `5-6 разряда` в курсе предусмотрены дополнительные материалы и отдельные точки углубления.
- В курсе мы разберем обязанности стропальщика, правила строповки, безопасное перемещение и складирование грузов, а также требования охраны труда.
- Главная задача курса — помочь выполнять работу безопасно, уверенно и без типовых ошибок.

Визуал:

- Крупное титульное фото: стропальщик, груз, кран, спокойный производственный фон.
- Внизу можно добавить короткую подпись: `Учебный курс по безопасным приемам работы`.

Интерактив:

- Кнопка `Начать курс` -> `S002`.

## `S002` Видеоинструкция по курсу и глосарий

Экран:

- `Видеоинструкция` - это навигация по курсу, откройте и ознакомьтесь.
- `Словарь терминов` - откройте и прочтите.

Озвучка:

- На этом экране слушатель сразу видит два полезных входа: в видеоинструкцию по прохождению курса и в словарь терминов.
- Видеоинструкция нужна, чтобы быстро понять навигацию по курсу и логику работы с материалом.
- Словарь терминов нужен как справочная опора, если в курсе встречаются незнакомые слова и обозначения.

Визуал:

- Экран из двух крупных блоков: `видеоинструкция` и `словарь терминов`.
- Для видео нужен понятный превью-кадр с кнопкой запуска.
- Для глосария нужен компактный список из нескольких терминов как пример содержания.

Интерактив:

- `Видеоинструкция` -> `S002-P01`.
- `Словарь терминов` -> `S002-P02`.
- `Далее` -> `S003`.

## `S002-P01` Видеоинструкция по курсу

Экран:

- `Видеоинструкция по курсу`
- Курс интерактивный
- Проходите темы по порядку
- После тем и блоков будут тесты
- По кнопкам открывайте пояснения
- Заметная плашка `НАЗАД К КУРСУ`

Озвучка:

- Этот курс интерактивный.
- Это значит, что вы не просто смотрите материал подряд.
- Вы будете переходить по слайдам, открывать дополнительные пояснения и отвечать на вопросы.
- Курс нужно проходить по порядку.
- Сначала идет Тема 1. Потом Тема 2. Потом Тема 3. После этого идет итоговое тестирование.
- Некоторые темы разбиты на более подробные блоки. Их тоже нужно проходить по порядку.
- После тем и отдельных блоков будут тесты для самопроверки.
- Если в слайде есть активная кнопка, нажмите на нее.
- Так вы откроете дополнительную информацию: термин, картинку, схему или пояснение.
- После просмотра нажмите `НАЗАД К КУРСУ` и продолжайте обучение.
- Если в промежуточном тесте ответ неверный, откроется ссылка на пояснение.
- Откройте ее, посмотрите материал, вернитесь к вопросу и продолжайте.
- В конце курса будет итоговый тест.
- В нем `20` вопросов.
- Допускается не больше `3` ошибок.
- В итоговом тесте подсказок и пояснений уже не будет.
- Если тест не сдан, его можно пройти повторно.
- При повторной попытке вопросы будут уже другими.
- Этот курс дает базу для стропальщика `2-4 разряда`.
- Если вы проходите подготовку на `5-6 разряд`, на отдельных слайдах увидите активную плашку `5-6 разряд - пройдите обучение`.
- Если такая плашка есть, откройте ее и изучите дополнительный материал.
- Для слушателя `2-4 разряда` этот дополнительный блок не обязателен.

Примечание по производству:

- этот текст можно утвердить заранее;
- для рабочей озвучки `S002-P01` сейчас берем файл `S002-P01_ozvuchka_55_sec_stress_2026-06-20.mp3`;
- сам видеоролик для `S002-P01` вставляем в финальной стадии проекта;
- к нему возвращаемся после готовности остальных блоков курса, тестов и окончательной навигации.

Визуал:

- Видеоплеер или крупный превью-кадр с кнопкой запуска.
- Отдельная заметная плашка `НАЗАД К КУРСУ`.

Интерактив:

- `НАЗАД К КУРСУ` -> `S002`.

## `S002-P02` Словарь терминов

Экран:

- `Словарь терминов`
- `Стропальщик` - рабочий, который выполняет строповку груза и участвует в его безопасном перемещении
- `Строп` - грузозахватное средство для подъема и перемещения груза
- `Строповка` - способ обвязки, зацепки или закрепления груза перед подъемом
- `Груз` - предмет, изделие или конструкция, которые нужно поднять и переместить
- `Грузозахватное приспособление` - устройство для захвата и удержания груза при подъеме
- `СИЗ` - средства индивидуальной защиты работника
- Заметная плашка `НАЗАД К КУРСУ`

Озвучка:

- Этот подвальный экран открывается из блока `Словарь терминов`.
- Здесь размещается словарь терминов курса в удобном справочном формате.
- Формулировки должны быть короткими, простыми и без перегруза специальной терминологией.
- При дальнейшем наполнении словарь можно расширять без перестройки линейного маршрута курса.

Визуал:

- Справочная карточка или таблица `термин / короткое объяснение`.
- Термины лучше разнести по двум колонкам, чтобы экран читался быстро.
- Отдельная заметная плашка `НАЗАД К КУРСУ`.

Интерактив:

- `НАЗАД К КУРСУ` -> `S002`.

## `S003` Какие документы регулируют работу стропальщика

Экран:

- Работу стропальщика регулируют:
- Трудовое законодательство
- Охрана труда
- Промышленная безопасность
- Инструкции работодателя

Нижняя подпись:

- Стропальщик работает по установленным правилам, а не `по памяти` и не `по привычке`.

Озвучка:

- Работа стропальщика относится к работам повышенной ответственности.
- Поэтому ее регулируют сразу несколько групп требований.
- Это общие требования трудового законодательства, правила охраны труда, требования промышленной безопасности и локальные инструкции организации.
- На практике это означает, что безопасная работа строится не на личной самоуверенности, а на четком соблюдении установленных правил.

Визуал:

- Схема из `4` равных блоков с иконками `закон / каска / кран / инструкция`.
- Внизу короткая линия-вывод.

Интерактив:

- `Далее` -> `S004`.
- Выносы:
  - `Трудовое законодательство` -> `S003-P01`
  - `Охрана труда` -> `S003-P02`
  - `Промышленная безопасность` -> `S003-P03`
  - `Инструкции работодателя` -> `S003-P04`

## `S003-P01` Трудовое законодательство: что это значит

Экран:

- Трудовое законодательство
- Определяет общие права и обязанности работника
- Устанавливает правила допуска к работе
- Требует безопасных условий труда

Озвучка:

- Для стропальщика это означает, что работник имеет не только обязанности, но и право на безопасные условия труда.
- Допуск к работе, обучение, инструктаж и организация рабочего места не являются формальностью.
- Это обязательная основа законной и безопасной работы.

Визуал:

- Справочная карточка с иконками `работник / документ / щит`.

Интерактив:

- `Вернуться` -> `S003`.

## `S003-P01-PP01` ТК РФ: пояснение для справки

Экран:

- Закон требует:
- Обучать работника безопасным приемам
- Допускать к работе только подготовленного работника
- Обеспечивать безопасные условия труда

Озвучка:

- Этот вынос нужен только как справка.
- Его задача — не загружать курс юридическими деталями, а показать простую логику: обучение, допуск и безопасность обязательны.

Визуал:

- Мини-карточка `обучение -> допуск -> работа`.

Интерактив:

- `Вернуться` -> `S003-P01`.

## `S003-P02` Охрана труда: что это значит

Экран:

- Охрана труда
- Это безопасные условия работы
- Это СИЗ
- Это правильные действия работника

Озвучка:

- Охрана труда для стропальщика — это не один документ и не один инструктаж.
- Это вся система безопасной работы: состояние рабочего места, применение СИЗ, соблюдение правил и отказ от опасных действий.

Визуал:

- Карточка с пиктограммами `каска / жилет / ботинки / безопасная зона`.

Интерактив:

- `Вернуться` -> `S003`.

## `S003-P03` Промышленная безопасность: что это значит

Экран:

- Промышленная безопасность
- Особенно важна при работе с грузоподъемными машинами
- Ошибка может привести к аварии
- Поэтому порядок работ должен быть строгим

Озвучка:

- При перемещении грузов ошибка влияет не только на одного работника, но и на всю опасную зону.
- Поэтому в работе с кранами и грузоподъемными механизмами особенно важны порядок действий, сигналы и контроль ситуации.

Визуал:

- Схема `кран -> груз -> опасная зона`.

Интерактив:

- `Вернуться` -> `S003`.

## `S003-P04` Инструкции работодателя: что это значит

Экран:

- Инструкции работодателя
- Уточняют, как работать именно на вашем объекте
- Определяют местный порядок допуска и взаимодействия
- Их нужно знать и соблюдать

Озвучка:

- Даже если работник знает общие правила, на каждом предприятии есть свой установленный порядок.
- Именно инструкции работодателя закрепляют, кто подает сигналы, как оформляется допуск, как организуется рабочее место и что делать в нештатной ситуации.

Визуал:

- Карточка `общие правила + местный порядок`.

Интерактив:

- `Вернуться` -> `S003`.

## `S004` Кто такой стропальщик

Экран:

- Стропальщик:
- Принимает и оценивает груз
- Выбирает строп и способ строповки
- Подает сигналы при перемещении
- Следит за безопасностью работы

Озвучка:

- Стропальщик — это работник, который подготавливает груз к безопасному перемещению.
- Он оценивает груз, выбирает способ строповки, контролирует правильность зацепки, участвует в подаче сигналов и следит за безопасностью людей в зоне работ.
- Его задача — не просто зацепить груз, а обеспечить безопасную и управляемую работу на всех этапах.

Визуал:

- Фото рабочего процесса плюс простая схема `груз -> строповка -> сигнал -> перемещение -> установка`.

Интерактив:

- `Далее` -> `S005`.
- Вынос `Что делает стропальщик по этапам` -> `S004-P01`.

## `S004-P01` Что делает стропальщик по этапам

Экран:

- До подъема:
- Оценивает груз и условия работы
- Во время перемещения:
- Контролирует безопасность и сигналы
- После установки:
- Убеждается в устойчивости груза

Озвучка:

- Работа стропальщика делится на три основных этапа.
- До подъема он оценивает груз, оснастку и условия.
- Во время перемещения контролирует безопасное сопровождение груза.
- После установки проверяет устойчивость груза и только затем завершает операцию.

Визуал:

- Мини-алгоритм из `3` этапов.

Интерактив:

- `Вернуться` -> `S004`.

## `S005` Кто может работать стропальщиком

Экран:

- Может работать стропальщиком тот, кто:
- Прошел обучение
- Прошел инструктаж
- Прошел проверку знаний
- Допущен к работе
- Использует СИЗ

Озвучка:

- К работе стропальщиком допускают не любого работника.
- Для этого нужно пройти обучение, инструктаж, проверку знаний и установленный порядок допуска.
- Кроме этого, работник должен знать безопасные приемы работы, понимать сигналы и использовать средства индивидуальной защиты.

Визуал:

- Чек-лист допуска с иконками `обучение / инструктаж / проверка / допуск / СИЗ`.

Интерактив:

- `Далее` -> `S006`.
- Выносы:
  - `Что такое инструктаж` -> `S005-P01`
  - `Что такое проверка знаний` -> `S005-P02`
  - `Что входит в СИЗ` -> `S005-P03`

## `S005-P01` Что такое инструктаж

Экран:

- Инструктаж
- Объясняет безопасный порядок работы
- Напоминает опасности и запреты
- Подтверждает готовность к работе

Озвучка:

- Инструктаж нужен, чтобы работник понимал конкретный безопасный порядок действий.
- Его цель — не формально расписаться, а разобраться, какие риски есть в работе и как их исключать.

Визуал:

- Справочная карточка `объяснили -> понял -> применяешь`.

Интерактив:

- `Вернуться` -> `S005`.

## `S005-P02` Что такое проверка знаний

Экран:

- Проверка знаний
- Показывает, что работник понял требования
- Подтверждает готовность к допуску
- Не является формальностью

Озвучка:

- Проверка знаний нужна для того, чтобы убедиться: работник действительно понимает правила, а не просто слышал их раньше.
- Для стропальщика это особенно важно, потому что ошибка в работе может привести к травме или аварийной ситуации.

Визуал:

- Карточка `знание -> допуск -> работа`.

Интерактив:

- `Вернуться` -> `S005`.

## `S005-P03` Что входит в СИЗ стропальщика

Экран:

- Базовые СИЗ стропальщика:
- Защитная каска
- Спецодежда
- Защитная обувь
- Перчатки и другие средства по условиям работы

Озвучка:

- Набор СИЗ зависит от условий конкретного объекта, но базовый принцип всегда один: работник должен быть защищен от типовых производственных рисков.
- СИЗ не заменяют правильную организацию работ, но являются обязательной частью безопасной работы.

Визуал:

- Иконки или фото `каска / жилет / ботинки / перчатки`.

Интерактив:

- `Вернуться` -> `S005`.

## `S006` Разряды `2-4`

Экран:

- Разряды, которые закрывает курс:
- `2 разряд` — простые грузы до `5 т`
- `3 разряд` — простые грузы свыше `5 до 25 т`
- `4 разряд` — сложные грузы до `5 т`

Нижняя подпись:

- Этот курс дает базу для работы стропальщика `2-4 разряда`.

Озвучка:

- Вводная часть курса ориентирована на подготовку стропальщиков `2-4 разряда`.
- Это базовый практический уровень, на котором особенно важно хорошо понимать порядок строповки, перемещения и безопасной работы.
- Для более высокого уровня ответственности в курсе предусмотрена отдельная надстройка `5-6 разряд`.

Визуал:

- Короткая таблица из `3` строк с цветовым делением по разрядам.

Интерактив:

- `Далее` -> `S007`.
- Вынос `Как понимать простые и сложные грузы` -> `S006-P01`.
- Плашка `Стропальщик 5-6 разряд` -> `S006-P56-01`.

## `S006-P01` Как понимать `простые`, `средней сложности`, `сложные грузы`

Экран:

- Простые грузы:
- Обычная форма и понятная схема строповки
- Сложные грузы:
- Смещенный центр тяжести, нестандартная форма, повышенная ответственность

Озвучка:

- Здесь важно понять общий принцип.
- Чем сложнее форма груза, чем выше риск ошибки при строповке и чем больше последствия ошибки, тем выше требования к квалификации и организации работ.

Визуал:

- Сравнительная карточка `простой груз / сложный груз`.

Интерактив:

- `Вернуться` -> `S006`.

## `S006-P56-01` Стропальщик `5-6 разряд`: что добавляется к базе

Экран:

- Стропальщик `5-6 разряд`
- Это надстройка над базой `2-4`
- Более тяжелые и ответственные грузы
- Более высокая цена ошибки

Озвучка:

- Ветка `5-6 разряд` не заменяет базовый курс, а дополняет его.
- Здесь речь идет о более тяжелых, более сложных и более ответственных грузах, где требования к опыту, внимательности и организации работ становятся выше.

Визуал:

- Контрастная плашка другого цветового кода, чтобы сразу было видно, что это отдельный уровень.

Интерактив:

- Кнопка `Подробнее` -> `S006-P56-01-PP01`.
- Кнопка `Вернуться в основной курс` -> `S006`.

## `S006-P56-01-PP01` Что относится к зоне `5-6 разряда`

Экран:

- К уровню `5-6 разряда` относятся:
- Более тяжелые грузы
- Особо ответственные изделия
- Сложные длинномерные и нестандартные грузы
- Работы с более высокой ценой ошибки

Озвучка:

- Этот экран нужен как ориентир.
- Его задача — показать, что переход к `5-6 разряду` связан не просто с цифрой в удостоверении, а с ростом сложности, массы груза и ответственности за результат.

Визуал:

- Справочная таблица или карточки по типам более сложных грузов.

Интерактив:

- `Вернуться` -> `S006-P56-01`.

## Практический вывод

Теперь `Тема 1` у нас подготовлена как готовый пакет содержимого, а не только как схема:

- есть экранный текст;
- есть готовая логика озвучки;
- есть указание по визуалу;
- есть переходы для линейных и справочных слайдов.

Следующий удобный шаг — так же подробно заполнить `Тему 2.1. Строповка грузов`.
