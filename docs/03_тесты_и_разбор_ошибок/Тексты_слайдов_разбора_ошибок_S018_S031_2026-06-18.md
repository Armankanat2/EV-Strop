# Тексты слайдов разбора ошибок `S019-P01...P12` и `S032-P01...P12`

Дата фиксации: `2026-06-18`

## Назначение

Это рабочий документ с готовыми текстами для подвальных слайдов разбора ошибок в промежуточных тестах:

- `S019` — Тема `2.1. Строповка грузов`;
- `S032` — Тема `2.2. Технология работы стропальщика`.

Формат такой же, как в документе экранных тезисов:

- `Экран` — короткий текст для показа на слайде;
- `Акцент озвучки` — пояснение для диктора;
- `Кнопка` — обязательная обратная ссылка в тест.

## Тест `S019` — Тема `2.1. Строповка грузов`

### `S019-P01` Ошибка: нет проверки груза перед подъемом

Экран:

- Ошибка: подъем начинают слишком рано
- Сначала оценивают груз
- Проверяют массу
- Проверяют схему строповки
- Проверяют условия работы

Акцент озвучки:

- Безопасная строповка начинается не с сигнала крановщику, а с оценки груза и условий его подъема.

Кнопка:

- `Вернуться к тесту`

### `S019-P02` Ошибка: работа с неизвестной массой груза

Экран:

- Масса груза неизвестна
- Подъем выполнять нельзя
- Сначала уточняют данные
- Потом выбирают строп и схему

Акцент озвучки:

- Если масса груза не подтверждена, стропальщик обязан остановить работу до уточнения характеристик.

Кнопка:

- `Вернуться к тесту`

### `S019-P03` Ошибка: не учтен центр тяжести

Экран:

- Устойчивость груза зависит от центра тяжести
- Смещенный центр тяжести опасен
- Схему строповки подбирают с учетом баланса

Акцент озвучки:

- Если не учитывать положение центра тяжести, груз может наклониться, сорваться или пойти в опасный разворот.

Кнопка:

- `Вернуться к тесту`

### `S019-P04` Ошибка: не учтен угол между ветвями стропа

Экран:

- Угол между ветвями важен
- Чем больше угол
- Тем выше нагрузка на ветви
- Схему выбирают с учетом допустимой нагрузки

Акцент озвучки:

- Ошибка в оценке угла между ветвями приводит к перегрузке стропа даже при внешне обычной строповке.

Кнопка:

- `Вернуться к тесту`

### `S019-P05` Ошибка: нет защиты от острых ребер

Экран:

- Острые ребра повреждают строп
- Нужны прокладки
- Нужна защита в местах контакта
- Подъем без защиты недопустим

Акцент озвучки:

- Если ветвь стропа работает по острой кромке без защиты, риск повреждения возникает еще до начала нормального перемещения груза.

Кнопка:

- `Вернуться к тесту`

### `S019-P06` Ошибка: не проверена зацепка и оснастка

Экран:

- Перед подъемом проверяют зацепку
- Проверяют состояние стропов
- Проверяют исправность оснастки
- Только после этого дают сигнал

Акцент озвучки:

- Правильная зацепка и пригодность оснастки подтверждаются до подъема, а не в момент движения груза.

Кнопка:

- `Вернуться к тесту`

### `S019-P07` Ошибка: работа не остановлена при опасности

Экран:

- Опасность выявлена
- Работу нужно остановить
- Нельзя продолжать подъем на сомнительной схеме
- Сначала устраняют риск

Акцент озвучки:

- Обязанность стропальщика не только выполнять операцию, но и вовремя остановить ее, если безопасный подъем не обеспечен.

Кнопка:

- `Вернуться к тесту`

### `S019-P08` Ошибка: сомнительная зацепка принята как допустимая

Экран:

- Сомнительная зацепка — типовая ошибка
- Нельзя работать "на глаз"
- Нужна проверенная схема
- Нужна надежная обвязка

Акцент озвучки:

- Если способ зацепки вызывает сомнение, его нельзя считать допустимым только потому, что он уже "похож на рабочий".

Кнопка:

- `Вернуться к тесту`

### `S019-P09` Ошибка: игнорируются манипуляционные знаки

Экран:

- Манипуляционные знаки важны
- Они показывают способ обращения с грузом
- Помогают выбрать безопасную строповку
- Помогают избежать ошибок при перемещении

Акцент озвучки:

- Знаки на грузе нужны не для формальности, а для правильного выбора способа подъема, перемещения и укладки.

Кнопка:

- `Вернуться к тесту`

### `S019-P10` Ошибка: проверка схемы считается необязательной

Экран:

- Проверка схемы обязательна
- Ее выполняют до сигнала на подъем
- Ее не переносят "на потом"
- Без проверки начинать работу нельзя

Акцент озвучки:

- Проверка схемы строповки является частью подготовки к работе и не заменяется пробным подъемом.

Кнопка:

- `Вернуться к тесту`

### `S019-P11` Ошибка: груз поднимают без подтвержденной массы

Экран:

- Нет маркировки массы
- Нельзя поднимать груз "наверняка"
- Нельзя заменять расчет предположением
- Сначала уточняют данные

Акцент озвучки:

- Более мощный строп не отменяет требования знать массу груза и понимать допустимую схему строповки.

Кнопка:

- `Вернуться к тесту`

### `S019-P12` Ошибка: ветвь идет по острому ребру без защиты

Экран:

- Ветвь стропа идет по острой кромке
- Это риск повреждения
- Подъем нужно остановить
- Сначала ставят защиту

Акцент озвучки:

- Даже если вся остальная схема выглядит рабочей, один опасный контакт со стропом делает подъем недопустимым.

Кнопка:

- `Вернуться к тесту`

## Тест `S032` — Тема `2.2. Технология работы стропальщика`

### `S032-P01` Ошибка: нет подготовки к работе

Экран:

- Подготовка к работе обязательна
- Уточняют задание
- Проверяют условия работы
- Осматривают оснастку

Акцент озвучки:

- Работа стропальщика начинается с организации и проверки условий, а не с немедленного начала перемещения груза.

Кнопка:

- `Вернуться к тесту`

### `S032-P02` Ошибка: работа начинается при плохой освещенности

Экран:

- Освещенность недостаточна
- Работу начинать нельзя
- Сначала устраняют небезопасные условия
- Потом продолжают операцию

Акцент озвучки:

- Плохая видимость мешает оценивать зацепку, сигналы, опасную зону и положение груза, поэтому работа при таких условиях недопустима.

Кнопка:

- `Вернуться к тесту`

### `S032-P03` Ошибка: не проверена обвязка перед подъемом

Экран:

- До подъема проверяют обвязку
- Проверяют зацепку
- Проверяют надежность строповки
- Только потом подают сигнал

Акцент озвучки:

- Ошибка в обвязке должна быть обнаружена до движения груза, иначе риск переходит в реальную аварийную ситуацию.

Кнопка:

- `Вернуться к тесту`

### `S032-P04` Ошибка: сигналы подает неуполномоченный работник

Экран:

- Сигналы подают по установленному порядку
- Не любой работник
- А назначенное лицо
- Это исключает путаницу

Акцент озвучки:

- Несогласованная подача команд создает риск неправильного перемещения и потери управления операцией.

Кнопка:

- `Вернуться к тесту`

### `S032-P05` Ошибка: команда `Стоп` недооценена

Экран:

- Команда `Стоп` обязательна
- Ее выполняют немедленно
- Независимо от того
- Кто ее подал

Акцент озвучки:

- В ситуации риска команда `Стоп` имеет приоритет, потому что ее задача — немедленно прервать опасное действие.

Кнопка:

- `Вернуться к тесту`

### `S032-P06` Ошибка: работа продолжается при неясном сигнале

Экран:

- Сигнал неясен
- Видимость недостаточна
- Работу нужно остановить
- Взаимодействие должно быть понятным

Акцент озвучки:

- Продолжать работу по догадке нельзя: при неясном сигнале или плохой видимости сначала восстанавливают безопасное управление.

Кнопка:

- `Вернуться к тесту`

### `S032-P07` Ошибка: оттяжка используется неправильно

Экран:

- Оттяжка нужна для сопровождения груза
- Она удерживает груз от раскачивания
- Помогает работать на дистанции
- Не заменяет правильную строповку

Акцент озвучки:

- Оттяжка нужна для безопасного контроля груза, а не для подтягивания его руками под подвесом.

Кнопка:

- `Вернуться к тесту`

### `S032-P08` Ошибка: человек находится под грузом или в опасной зоне

Экран:

- Под грузом находиться нельзя
- Опасную зону контролируют заранее
- Путь перемещения должен быть свободен
- Безопасность людей приоритетна

Акцент озвучки:

- Главная задача перед перемещением — исключить нахождение людей под грузом и рядом с возможной траекторией его падения.

Кнопка:

- `Вернуться к тесту`

### `S032-P09` Ошибка: место укладки не подготовлено

Экран:

- Место укладки готовят заранее
- Проверяют устойчивость
- Проверяют подкладки
- Груз должен лечь безопасно

Акцент озвучки:

- Укладка считается безопасной только тогда, когда место заранее подготовлено под массу, форму и устойчивость груза.

Кнопка:

- `Вернуться к тесту`

### `S032-P10` Ошибка: расстроповка начата слишком рано

Экран:

- Расстроповка не делается сразу
- Сначала груз ставят устойчиво
- Проверяют окончательное положение
- Потом снимают стропы

Акцент озвучки:

- Если груз еще не занял устойчивое положение, расстроповка создает риск смещения, опрокидывания и зажатия оснастки.

Кнопка:

- `Вернуться к тесту`

### `S032-P11` Ошибка: не остановлено перемещение при раскачивании и людях в зоне

Экран:

- Груз раскачивается
- В зоне появились люди
- Перемещение нужно остановить
- Людей выводят из опасной зоны

Акцент озвучки:

- При совмещении двух рисков, раскачивания груза и присутствия людей, операция должна быть немедленно остановлена.

Кнопка:

- `Вернуться к тесту`

### `S032-P12` Ошибка: расстроповка при неустойчивом положении груза

Экран:

- Груз стоит неустойчиво
- Расстроповка недопустима
- Сначала выравнивают груз
- Затем обеспечивают устойчивость

Акцент озвучки:

- Нельзя освобождать стропы, пока груз не установлен надежно и не исключено его смещение или опрокидывание.

Кнопка:

- `Вернуться к тесту`

## Практический вывод

Теперь для `24` подвальных слайдов есть готовая текстовая основа:

- короткий экранный текст;
- ключевой акцент для озвучки;
- единая кнопка возврата в тест.

Следующий удобный шаг — собрать к этим слайдам визуальный реестр, особенно для `S019-P03`, `S019-P04`, `S019-P05`, `S019-P09`, `S032-P07`, `S032-P08`, `S032-P09`, `S032-P11`.
